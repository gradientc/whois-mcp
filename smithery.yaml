name: whois-mcp
description: MCP server for WHOIS lookups (domain, IP, TLD, ASN)
startCommand:
  type: stdio
  configSchema:
    type: object
    properties: {}
  commandFunction: |-
    (config) => ({ command: 'node', args: ['dist/index.js'] })
